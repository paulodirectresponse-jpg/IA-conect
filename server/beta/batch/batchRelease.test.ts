import fs from'fs';import path from'path';import{describe,expect,it}from'vitest';const read=(f:string)=>fs.readFileSync(path.join(process.cwd(),f),'utf8');describe('PR-17 Batch release',()=>{it('keeps beta.batch governed by release marker and admin kill switch',()=>{const f=read('server/services/featureFlagService.ts'),flags=read('src/beta/betaFlags.ts');expect(flags).toContain("flag_key: 'beta.batch'");expect(f).toContain('app_config/beta_batch_v1_release');expect(f).toContain("release:'PR-17_BATCH_V1'");});it('gates execution through beta.batch and central beta execution switch',()=>{const s=read('server/beta/batch/batchService.ts');expect(s).toContain("getFeatureFlag('beta.batch')");expect(s).toContain("getFeatureFlag('beta.execution.enabled')");});it('ships desktop and mobile Batch UI without provider or final pricing leakage',()=>{const ui=read('src/beta/views/BetaBatchView.tsx'),css=read('src/beta/styles/batch.css');expect(css).toContain('@media(max-width:900px)');expect(css).toContain('@media(max-width:560px)');expect(ui).not.toMatch(/wavespeed|atlas|provider_model_identifier|provider_id/i);expect(ui).not.toContain('credit_price');});});
