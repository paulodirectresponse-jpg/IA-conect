import crypto from 'crypto';
import { firestoreAdminRest } from '../repositories/firestoreAdminRest.js';
import { PackVersion } from './packCatalogService.js';

export type CouponBenefitType='BONUS_PERCENT'|'BONUS_FIXED'|'BRL_PERCENT'|'BRL_FIXED';
export interface CouponVersion{coupon_id:string;code:string;version:number;name:string;active:boolean;starts_at:string;expires_at:string|null;benefit_type:CouponBenefitType;benefit_value:number;eligible_pack_ids:string[];min_purchase_cents:number;max_purchase_cents:number|null;max_global_redemptions:number|null;max_redemptions_per_user:number;first_purchase_only:boolean;bonus_expires_days:number|null;budget_max_credits:number|null;created_at:string;created_by:string;notes:string;}
export interface CouponQuote{valid:true;coupon:CouponVersion;original_amount_cents:number;discount_cents:number;final_amount_cents:number;bonus_credits:number;total_credits:number;}
const HEADS='coupon_heads',VERSIONS='coupon_versions',USAGE='coupon_usage',USER_USAGE='coupon_user_usage',RES='coupon_reservations',REDEEM='coupon_redemptions';
const enc=(s:string)=>encodeURIComponent(s);const h=(s:string)=>crypto.createHash('sha256').update(s).digest('hex');
export function normalizeCouponCode(v:string){return String(v||'').trim().toUpperCase().replace(/[^A-Z0-9_-]/g,'');}
function versionId(code:string,version:number){return `${code}__v${version}`;}function userUsageId(code:string,version:number,userId:string){return h(`${code}:${version}:${userId}`);}
function nowIso(){return new Date().toISOString();}
function validateConfig(c:CouponVersion){if(!c.code||c.code.length<3)throw Object.assign(new Error('Código do cupom deve ter ao menos 3 caracteres.'),{code:'COUPON_CODE_INVALID'});if(!c.name.trim())throw new Error('Nome do cupom é obrigatório.');if(!['BONUS_PERCENT','BONUS_FIXED','BRL_PERCENT','BRL_FIXED'].includes(c.benefit_type))throw new Error('Tipo de benefício inválido.');if(!Number.isFinite(c.benefit_value)||c.benefit_value<=0)throw new Error('Valor do benefício deve ser maior que zero.');if(c.benefit_type.endsWith('PERCENT')&&c.benefit_value>100)throw new Error('Percentual não pode ultrapassar 100%.');if(c.expires_at&&new Date(c.expires_at).getTime()<=new Date(c.starts_at).getTime())throw new Error('Fim da campanha deve ser posterior ao início.');if(c.max_redemptions_per_user<1)throw new Error('Limite por usuário inválido.');}
async function confirmedPayments(userId:string){const rows=await firestoreAdminRest.runQuery({from:[{collectionId:'payments'}],where:{compositeFilter:{op:'AND',filters:[{fieldFilter:{field:{fieldPath:'user_id'},op:'EQUAL',value:{stringValue:userId}}},{fieldFilter:{field:{fieldPath:'status'},op:'EQUAL',value:{stringValue:'CONFIRMED'}}}]}},limit:1});return rows.length>0;}
async function currentByCode(raw:string):Promise<CouponVersion|null>{const code=normalizeCouponCode(raw);if(!code)return null;const head=await firestoreAdminRest.get(`${HEADS}/${enc(code)}`);if(!head.exists)return null;const v=Number(head.data?.current_version||0);if(!v)return null;const d=await firestoreAdminRest.get(`${VERSIONS}/${enc(versionId(code,v))}`);return d.exists?d.data as CouponVersion:null;}
function compute(c:CouponVersion,pack:PackVersion){let discount=0,bonus=0;if(c.benefit_type==='BONUS_PERCENT')bonus=Math.floor(pack.total_credits*c.benefit_value/100);if(c.benefit_type==='BONUS_FIXED')bonus=Math.floor(c.benefit_value);if(c.benefit_type==='BRL_PERCENT')discount=Math.floor(pack.price_brl_cents*c.benefit_value/100);if(c.benefit_type==='BRL_FIXED')discount=Math.floor(c.benefit_value);discount=Math.max(0,Math.min(pack.price_brl_cents-1,discount));return{discount,bonus:Math.max(0,bonus)};}
async function eligibility(userId:string,c:CouponVersion,pack:PackVersion){const now=Date.now();if(!c.active)throw Object.assign(new Error('Este cupom está pausado.'),{code:'COUPON_INACTIVE'});if(new Date(c.starts_at).getTime()>now)throw Object.assign(new Error('Este cupom ainda não está vigente.'),{code:'COUPON_NOT_STARTED'});if(c.expires_at&&new Date(c.expires_at).getTime()<=now)throw Object.assign(new Error('Este cupom expirou.'),{code:'COUPON_EXPIRED'});if(c.eligible_pack_ids.length&&!c.eligible_pack_ids.includes(pack.pack_id))throw Object.assign(new Error('Este cupom não é válido para o pack selecionado.'),{code:'COUPON_PACK_INELIGIBLE'});if(pack.price_brl_cents<c.min_purchase_cents)throw Object.assign(new Error('O valor deste pack é menor que o mínimo do cupom.'),{code:'COUPON_MIN_PURCHASE'});if(c.max_purchase_cents!=null&&pack.price_brl_cents>c.max_purchase_cents)throw Object.assign(new Error('O valor deste pack excede o máximo permitido pelo cupom.'),{code:'COUPON_MAX_PURCHASE'});if(c.first_purchase_only&&await confirmedPayments(userId))throw Object.assign(new Error('Este cupom é exclusivo para a primeira compra.'),{code:'COUPON_FIRST_PURCHASE_ONLY'});const usage=await firestoreAdminRest.get(`${USAGE}/${enc(versionId(c.code,c.version))}`),u=usage.exists?usage.data:{};if(c.max_global_redemptions!=null&&Number(u.reserved||0)+Number(u.redeemed||0)>=c.max_global_redemptions)throw Object.assign(new Error('O limite de utilizações deste cupom foi atingido.'),{code:'COUPON_GLOBAL_LIMIT'});if(c.budget_max_credits!=null&&Number(u.bonus_credits_committed||0)>=c.budget_max_credits)throw Object.assign(new Error('O orçamento promocional deste cupom foi encerrado.'),{code:'COUPON_BUDGET_EXHAUSTED'});const uud=await firestoreAdminRest.get(`${USER_USAGE}/${userUsageId(c.code,c.version,userId)}`),uu=uud.exists?uud.data:{};if(Number(uu.reserved||0)+Number(uu.redeemed||0)>=c.max_redemptions_per_user)throw Object.assign(new Error('Você já utilizou o limite permitido deste cupom.'),{code:'COUPON_USER_LIMIT'});return{usageDoc:usage,userUsageDoc:uud,usage:u,userUsage:uu};}

export const couponService={
 async quote(userId:string,code:string,pack:PackVersion):Promise<CouponQuote>{const c=await currentByCode(code);if(!c)throw Object.assign(new Error('Cupom inválido.'),{code:'COUPON_NOT_FOUND'});await eligibility(userId,c,pack);const {discount,bonus}=compute(c,pack);return{valid:true,coupon:c,original_amount_cents:pack.price_brl_cents,discount_cents:discount,final_amount_cents:pack.price_brl_cents-discount,bonus_credits:bonus,total_credits:pack.total_credits+bonus};},
 async reserveForPayment(userId:string,code:string,pack:PackVersion,paymentId:string){const existing=await firestoreAdminRest.get(`${RES}/${enc(paymentId)}`);if(existing.exists)return existing.data;for(let attempt=0;attempt<5;attempt++){const c=await currentByCode(code);if(!c)throw Object.assign(new Error('Cupom inválido.'),{code:'COUPON_NOT_FOUND'});const state=await eligibility(userId,c,pack);const {discount,bonus}=compute(c,pack);const usagePath=`${USAGE}/${enc(versionId(c.code,c.version))}`,userPath=`${USER_USAGE}/${userUsageId(c.code,c.version,userId)}`,now=nowIso();const usage={coupon_code:c.code,coupon_version:c.version,reserved:Number(state.usage.reserved||0)+1,redeemed:Number(state.usage.redeemed||0),reversed:Number(state.usage.reversed||0),bonus_credits_committed:Number(state.usage.bonus_credits_committed||0)+bonus,updated_at:now};if(c.budget_max_credits!=null&&usage.bonus_credits_committed>c.budget_max_credits)throw Object.assign(new Error('O orçamento promocional restante não comporta este uso.'),{code:'COUPON_BUDGET_EXHAUSTED'});const userUsage={user_id:userId,coupon_code:c.code,coupon_version:c.version,reserved:Number(state.userUsage.reserved||0)+1,redeemed:Number(state.userUsage.redeemed||0),reversed:Number(state.userUsage.reversed||0),updated_at:now};const reservation={payment_id:paymentId,user_id:userId,status:'RESERVED',coupon_snapshot:c,coupon_code:c.code,coupon_version:c.version,discount_cents:discount,bonus_credits:bonus,original_amount_cents:pack.price_brl_cents,final_amount_cents:pack.price_brl_cents-discount,pack_id:pack.pack_id,expires_at:new Date(Date.now()+30*60000).toISOString(),created_at:now,updated_at:now};try{await firestoreAdminRest.commit([{update:{name:firestoreAdminRest.docName(usagePath),fields:firestoreAdminRest.fields(usage)},currentDocument:state.usageDoc.exists?{updateTime:state.usageDoc.updateTime}:{exists:false}},{update:{name:firestoreAdminRest.docName(userPath),fields:firestoreAdminRest.fields(userUsage)},currentDocument:state.userUsageDoc.exists?{updateTime:state.userUsageDoc.updateTime}:{exists:false}},{update:{name:firestoreAdminRest.docName(`${RES}/${enc(paymentId)}`),fields:firestoreAdminRest.fields(reservation)},currentDocument:{exists:false}}]);return reservation;}catch(e){if((await firestoreAdminRest.get(`${RES}/${enc(paymentId)}`)).exists)return(await firestoreAdminRest.get(`${RES}/${enc(paymentId)}`)).data;if(attempt===4)throw e;}}throw new Error('Falha ao reservar cupom.');},
 async redeemPayment(paymentId:string){const path=`${RES}/${enc(paymentId)}`,rd=await firestoreAdminRest.get(path);if(!rd.exists)return null;const r:any=rd.data;if(r.status==='REDEEMED')return r;if(r.status!=='RESERVED')return r;for(let attempt=0;attempt<5;attempt++){const fresh=await firestoreAdminRest.get(path);if(!fresh.exists)return null;const cur:any=fresh.data;if(cur.status!=='RESERVED')return cur;const usagePath=`${USAGE}/${enc(versionId(cur.coupon_code,cur.coupon_version))}`,userPath=`${USER_USAGE}/${userUsageId(cur.coupon_code,cur.coupon_version,cur.user_id)}`,ud=await firestoreAdminRest.get(usagePath),uud=await firestoreAdminRest.get(userPath),u=ud.data||{},uu=uud.data||{},now=nowIso(),nextR={...cur,status:'REDEEMED',redeemed_at:now,updated_at:now},usage={...u,reserved:Math.max(0,Number(u.reserved||0)-1),redeemed:Number(u.redeemed||0)+1,updated_at:now},userUsage={...uu,reserved:Math.max(0,Number(uu.reserved||0)-1),redeemed:Number(uu.redeemed||0)+1,updated_at:now},redemption={payment_id:paymentId,user_id:cur.user_id,coupon_code:cur.coupon_code,coupon_version:cur.coupon_version,discount_cents:cur.discount_cents,bonus_credits:cur.bonus_credits,status:'REDEEMED',created_at:now};try{await firestoreAdminRest.commit([{update:{name:firestoreAdminRest.docName(path),fields:firestoreAdminRest.fields(nextR)},currentDocument:{updateTime:fresh.updateTime}},{update:{name:firestoreAdminRest.docName(usagePath),fields:firestoreAdminRest.fields(usage)},currentDocument:{updateTime:ud.updateTime}},{update:{name:firestoreAdminRest.docName(userPath),fields:firestoreAdminRest.fields(userUsage)},currentDocument:{updateTime:uud.updateTime}},{update:{name:firestoreAdminRest.docName(`${REDEEM}/${enc(paymentId)}`),fields:firestoreAdminRest.fields(redemption)},currentDocument:{exists:false}}]);return nextR;}catch(e){if(attempt===4)throw e;}}return null;},
 async releasePayment(paymentId:string){const path=`${RES}/${enc(paymentId)}`;for(let attempt=0;attempt<5;attempt++){const rd=await firestoreAdminRest.get(path);if(!rd.exists)return;if(rd.data?.status!=='RESERVED')return;const r:any=rd.data,usagePath=`${USAGE}/${enc(versionId(r.coupon_code,r.coupon_version))}`,userPath=`${USER_USAGE}/${userUsageId(r.coupon_code,r.coupon_version,r.user_id)}`,ud=await firestoreAdminRest.get(usagePath),uud=await firestoreAdminRest.get(userPath),u=ud.data||{},uu=uud.data||{},now=nowIso();try{await firestoreAdminRest.commit([{update:{name:firestoreAdminRest.docName(path),fields:firestoreAdminRest.fields({...r,status:'RELEASED',updated_at:now})},currentDocument:{updateTime:rd.updateTime}},{update:{name:firestoreAdminRest.docName(usagePath),fields:firestoreAdminRest.fields({...u,reserved:Math.max(0,Number(u.reserved||0)-1),bonus_credits_committed:Math.max(0,Number(u.bonus_credits_committed||0)-Number(r.bonus_credits||0)),updated_at:now})},currentDocument:{updateTime:ud.updateTime}},{update:{name:firestoreAdminRest.docName(userPath),fields:firestoreAdminRest.fields({...uu,reserved:Math.max(0,Number(uu.reserved||0)-1),updated_at:now})},currentDocument:{updateTime:uud.updateTime}}]);return;}catch(e){if(attempt===4)throw e;}}},
 async reversePayment(paymentId:string){const red=await firestoreAdminRest.get(`${REDEEM}/${enc(paymentId)}`);if(!red.exists||red.data?.status==='REVERSED')return;const r:any=red.data,usagePath=`${USAGE}/${enc(versionId(r.coupon_code,r.coupon_version))}`,userPath=`${USER_USAGE}/${userUsageId(r.coupon_code,r.coupon_version,r.user_id)}`,ud=await firestoreAdminRest.get(usagePath),uud=await firestoreAdminRest.get(userPath),u=ud.data||{},uu=uud.data||{},now=nowIso();await firestoreAdminRest.commit([{update:{name:firestoreAdminRest.docName(`${REDEEM}/${enc(paymentId)}`),fields:firestoreAdminRest.fields({...r,status:'REVERSED',reversed_at:now})},currentDocument:{updateTime:red.updateTime}},{update:{name:firestoreAdminRest.docName(usagePath),fields:firestoreAdminRest.fields({...u,redeemed:Math.max(0,Number(u.redeemed||0)-1),reversed:Number(u.reversed||0)+1,updated_at:now})},currentDocument:{updateTime:ud.updateTime}},{update:{name:firestoreAdminRest.docName(userPath),fields:firestoreAdminRest.fields({...uu,redeemed:Math.max(0,Number(uu.redeemed||0)-1),reversed:Number(uu.reversed||0)+1,updated_at:now})},currentDocument:{updateTime:uud.updateTime}}]);},
 async listAdmin(){const heads=await firestoreAdminRest.runQuery({from:[{collectionId:HEADS}],limit:200});const out=[] as any[];for(const row of heads){const code=String(row.data.code),version=Number(row.data.current_version),d=await firestoreAdminRest.get(`${VERSIONS}/${enc(versionId(code,version))}`);if(!d.exists)continue;const u=await firestoreAdminRest.get(`${USAGE}/${enc(versionId(code,version))}`);out.push({...d.data,usage:u.exists?u.data:{reserved:0,redeemed:0,reversed:0,bonus_credits_committed:0}});}return out.sort((a,b)=>new Date(b.created_at).getTime()-new Date(a.created_at).getTime());},
 async publish(input:any,adminId:string){const code=normalizeCouponCode(input.code);const headPath=`${HEADS}/${enc(code)}`,hd=await firestoreAdminRest.get(headPath),version=Number(hd.data?.current_version||0)+1,now=nowIso();const c:CouponVersion={coupon_id:versionId(code,version),code,version,name:String(input.name||code),active:input.active!==false,starts_at:new Date(input.starts_at||now).toISOString(),expires_at:input.expires_at?new Date(input.expires_at).toISOString():null,benefit_type:input.benefit_type,benefit_value:Number(input.benefit_value),eligible_pack_ids:Array.isArray(input.eligible_pack_ids)?input.eligible_pack_ids.map(String):[],min_purchase_cents:Math.max(0,Number(input.min_purchase_cents||0)),max_purchase_cents:input.max_purchase_cents==null||input.max_purchase_cents===''?null:Number(input.max_purchase_cents),max_global_redemptions:input.max_global_redemptions==null||input.max_global_redemptions===''?null:Number(input.max_global_redemptions),max_redemptions_per_user:Math.max(1,Number(input.max_redemptions_per_user||1)),first_purchase_only:Boolean(input.first_purchase_only),bonus_expires_days:input.bonus_expires_days==null||input.bonus_expires_days===''?null:Number(input.bonus_expires_days),budget_max_credits:input.budget_max_credits==null||input.budget_max_credits===''?null:Number(input.budget_max_credits),created_at:now,created_by:adminId,notes:String(input.notes||'')};validateConfig(c);await firestoreAdminRest.commit([{update:{name:firestoreAdminRest.docName(`${VERSIONS}/${enc(c.coupon_id)}`),fields:firestoreAdminRest.fields(c)},currentDocument:{exists:false}},{update:{name:firestoreAdminRest.docName(headPath),fields:firestoreAdminRest.fields({code,current_version:version,updated_at:now})},currentDocument:hd.exists?{updateTime:hd.updateTime}:{exists:false}}]);return c;},
 async setActive(code:string,active:boolean,adminId:string){const current=await currentByCode(code);if(!current)throw Object.assign(new Error('Cupom não encontrado.'),{code:'COUPON_NOT_FOUND'});return this.publish({...current,active,code:current.code},adminId);}
};
