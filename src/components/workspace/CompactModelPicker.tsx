import React,{useEffect,useMemo,useRef,useState} from 'react';
import {ModelRegistryItem,PricingEntry,GenerationMode} from '../../types/index.js';
import {ChevronDown,Search,Star,Check} from 'lucide-react';
import {formatCentsToBRL} from '../../config/constants.js';
interface Props{models:ModelRegistryItem[];pricing:PricingEntry[];selectedModelId:string;onSelectModel:(m:ModelRegistryItem)=>void;favoriteModelIds:string[];recentModelIds:string[];onToggleFavorite:(id:string)=>void;currentResolution:string;currentMode:GenerationMode;currentDuration:number;currentOutputs:number;}
export const CompactModelPicker:React.FC<Props>=({models,pricing,selectedModelId,onSelectModel,favoriteModelIds,recentModelIds,onToggleFavorite,currentResolution,currentDuration,currentOutputs})=>{const[open,setOpen]=useState(false);const[search,setSearch]=useState('');const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const h=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener('mousedown',h);return()=>document.removeEventListener('mousedown',h)},[]);const selected=models.find(m=>m.model_id===selectedModelId)||models[0];const price=(id:string)=>{const rows=pricing.filter(p=>p.active&&p.model_id===id&&p.resolution.toLowerCase()===currentResolution.toLowerCase());if(!rows.length)return'—';const totals=rows.map(p=>Math.ceil(p.customer_price_cents*(currentDuration/Math.max(1,p.duration_seconds||1))*Math.max(1,currentOutputs)));return formatCentsToBRL(Math.min(...totals));};const filtered=useMemo(()=>models.filter(m=>!search.trim()||m.name.toLowerCase().includes(search.toLowerCase())||m.description.toLowerCase().includes(search.toLowerCase())),[models,search]);const sorted=[...filtered].sort((a,b)=>Number(favoriteModelIds.includes(b.model_id))-Number(favoriteModelIds.includes(a.model_id))||Number(recentModelIds.includes(b.model_id))-Number(recentModelIds.includes(a.model_id))||a.name.localeCompare(b.name));return <div className="relative" ref={ref}><label className="block text-xs font-semibold text-zinc-700 mb-1.5">Model</label><button type="button" onClick={()=>setOpen(!open)} className="w-full flex items-center justify-between px-3 py-2.5 bg-white border border-zinc-200 hover:border-zinc-300 rounded-xl text-left"><div className="min-w-0"><p className="font-semibold text-xs truncate">{selected?.name||'Select model'}</p><p className="text-[10px] text-zinc-400 truncate">{selected?.best_for||selected?.description}</p></div><div className="flex items-center gap-2"><span className="font-mono text-xs font-semibold text-emerald-700">{selected?price(selected.model_id):'—'}</span><ChevronDown className={`w-4 h-4 text-zinc-400 ${open?'rotate-180':''}`}/></div></button>{open&&<div className="absolute z-50 top-full mt-1.5 inset-x-0 bg-white border border-zinc-200 rounded-2xl shadow-xl overflow-hidden"><div className="p-2 border-b border-zinc-100"><div className="relative"><Search className="w-3.5 h-3.5 absolute left-2.5 top-1/2 -translate-y-1/2 text-zinc-400"/><input autoFocus value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search models" className="w-full pl-8 pr-3 py-2 text-xs border border-zinc-200 rounded-lg outline-none focus:border-zinc-400"/></div></div><div className="max-h-80 overflow-y-auto p-1.5">{sorted.map(m=>{const fav=favoriteModelIds.includes(m.model_id);return <div key={m.model_id} onClick={()=>{onSelectModel(m);setOpen(false)}} className={`flex items-center gap-2 px-2 py-2 rounded-xl cursor-pointer ${m.model_id===selectedModelId?'bg-emerald-50':'hover:bg-zinc-50'}`}><button type="button" onClick={e=>{e.stopPropagation();onToggleFavorite(m.model_id)}} className={fav?'text-amber-500':'text-zinc-300'}><Star className={`w-3.5 h-3.5 ${fav?'fill-current':''}`}/></button><div className="min-w-0 flex-1"><p className="text-xs font-semibold truncate">{m.name}</p><p className="text-[10px] text-zinc-400 truncate">{m.best_for||m.description}</p></div><span className="font-mono text-xs text-emerald-700">{price(m.model_id)}</span>{m.model_id===selectedModelId&&<Check className="w-3.5 h-3.5 text-emerald-600"/>}</div>})}{!sorted.length&&<p className="p-6 text-center text-xs text-zinc-400">No models found.</p>}</div></div>}</div>};
