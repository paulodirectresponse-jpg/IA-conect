import React, { useEffect, useMemo, useRef, useState } from 'react';
import { Sparkles, ChevronDown, ChevronUp, Upload, Image as ImageIcon, Video, Music, Loader2 } from 'lucide-react';
import { Asset, WorkspaceReference } from '../../types/index.js';
import { assetService } from '../../services/assetService.js';

interface PromptComposerProps {prompt:string;onChangePrompt:(text:string)=>void;negativePrompt:string;onChangeNegativePrompt:(text:string)=>void;onOpenImproveModal:()=>void;references:WorkspaceReference[];supportsNegativePrompt?:boolean;maxChars?:number;}
const makeName=(file:File)=>file.name.replace(/\.[^/.]+$/,'').replace(/[_-]+/g,' ').replace(/\s+/g,' ').trim();
export const PromptComposer:React.FC<PromptComposerProps>=({prompt,onChangePrompt,negativePrompt,onChangeNegativePrompt,onOpenImproveModal,references,supportsNegativePrompt=true,maxChars=2000})=>{
 const[showNegative,setShowNegative]=useState(Boolean(negativePrompt));const[mentionOpen,setMentionOpen]=useState(false);const[mentionQuery,setMentionQuery]=useState('');const[mentionStart,setMentionStart]=useState<number|null>(null);const[uploading,setUploading]=useState(false);const[assets,setAssets]=useState<Asset[]>([]);const textareaRef=useRef<HTMLTextAreaElement>(null);const fileRef=useRef<HTMLInputElement>(null);
 useEffect(()=>{assetService.listAssets().then(setAssets).catch(()=>setAssets([]));},[]);
 const suggestions=useMemo(()=>{const q=mentionQuery.toLowerCase();return assets.filter(a=>!q||a.name.toLowerCase().includes(q)||a.alias.toLowerCase().includes(q)).slice(0,8)},[assets,mentionQuery]);
 const attach=(asset:Asset)=>{if(!references.some(r=>r.asset_id===asset.asset_id)){references.push({asset_id:asset.asset_id,alias_snapshot:asset.alias,priority:'HIGH',preservation_rules:[],flexible_rules:[],asset});}}
 const detect=(value:string,caret:number)=>{const before=value.slice(0,caret);const m=before.match(/(?:^|\s)@([\w-]*)$/);if(!m){setMentionOpen(false);setMentionStart(null);return}setMentionQuery(m[1]||'');setMentionStart(caret-(m[1]?.length||0)-1);setMentionOpen(true)};
 const insert=(asset:Asset)=>{const t=textareaRef.current;const caret=t?.selectionStart??prompt.length;const start=mentionStart??caret;const token=`@${asset.alias} `;onChangePrompt(prompt.slice(0,start)+token+prompt.slice(caret));attach(asset);setMentionOpen(false);setMentionQuery('');setTimeout(()=>{t?.focus();const pos=start+token.length;t?.setSelectionRange(pos,pos)},0)};
 const quickUpload=async(file?:File)=>{if(!file)return;setUploading(true);try{const asset=await assetService.uploadAsset({file,name:makeName(file)||file.name});setAssets(p=>[asset,...p]);insert(asset)}finally{setUploading(false);if(fileRef.current)fileRef.current.value=''}};
 const icon=(a:Asset)=>a.type==='VIDEO'?<Video className="w-4 h-4"/>:a.type==='AUDIO'?<Music className="w-4 h-4"/>:<ImageIcon className="w-4 h-4"/>;
 return <div className="space-y-2"><div className="flex items-center justify-between"><label className="text-xs font-semibold text-zinc-700">Prompt</label><button type="button" onClick={onOpenImproveModal} disabled={!prompt.trim()} className="text-[11px] font-medium text-emerald-700 disabled:opacity-40 flex items-center gap-1"><Sparkles className="w-3 h-3"/>Improve prompt</button></div><div className="relative"><textarea ref={textareaRef} value={prompt} onChange={e=>{onChangePrompt(e.target.value);detect(e.target.value,e.target.selectionStart)}} onClick={e=>detect(prompt,(e.target as HTMLTextAreaElement).selectionStart)} onKeyUp={e=>{if(e.key==='Escape')setMentionOpen(false)}} placeholder="Describe your video. Type @ to reference an asset instantly..." rows={5} maxLength={maxChars} className="w-full p-3 bg-white border border-zinc-200 hover:border-zinc-300 focus:border-emerald-600 rounded-xl text-xs text-zinc-900 placeholder:text-zinc-400 focus:outline-none resize-none leading-relaxed shadow-2xs"/>{mentionOpen&&<div className="absolute z-[80] left-2 right-2 top-full mt-1 bg-white border border-zinc-200 rounded-xl shadow-2xl overflow-hidden"><div className="px-3 py-2 border-b border-zinc-100 flex items-center justify-between"><span className="text-[10px] font-semibold text-zinc-500">Assets</span><button type="button" disabled={uploading} onClick={()=>fileRef.current?.click()} className="text-[10px] font-semibold text-emerald-700 flex items-center gap-1">{uploading?<Loader2 className="w-3 h-3 animate-spin"/>:<Upload className="w-3 h-3"/>} Novo asset</button></div><div className="max-h-64 overflow-y-auto p-1.5">{suggestions.map(a=><button key={a.asset_id} type="button" onClick={()=>insert(a)} className="w-full flex items-center gap-2 p-2 rounded-lg hover:bg-zinc-50 text-left"><div className="w-10 h-10 rounded-lg bg-zinc-100 overflow-hidden shrink-0 flex items-center justify-center text-zinc-400">{(a.thumbnail_url||a.public_url)&&a.type==='IMAGE'?<img src={a.thumbnail_url||a.public_url} className="w-full h-full object-cover" alt=""/>:icon(a)}</div><div className="min-w-0"><p className="text-xs font-semibold truncate">{a.name}</p><p className="text-[10px] font-mono text-emerald-700 truncate">@{a.alias}</p></div></button>)}{!suggestions.length&&<div className="p-4 text-center text-[11px] text-zinc-400">Nenhum asset encontrado. Envie um sem sair do prompt.</div>}</div></div>}<input ref={fileRef} type="file" className="hidden" accept="image/*,video/*,audio/*" onChange={e=>quickUpload(e.target.files?.[0])}/><div className="flex justify-between mt-1 text-[10px] text-zinc-400"><span>Digite @ para abrir seus assets</span><span className="font-mono">{prompt.length} / {maxChars}</span></div></div>{references.length>0&&<div className="flex gap-1.5 overflow-x-auto">{references.map(r=><span key={r.asset_id} className="text-[10px] font-mono px-1.5 py-0.5 rounded-md bg-emerald-50 text-emerald-700 border border-emerald-200/60">@{r.alias_snapshot}</span>)}</div>}{supportsNegativePrompt&&<div><button type="button" onClick={()=>setShowNegative(!showNegative)} className="text-[11px] text-zinc-500 flex items-center gap-1 py-0.5">{showNegative?<ChevronUp className="w-3 h-3"/>:<ChevronDown className="w-3 h-3"/>}Negative prompt</button>{showNegative&&<textarea value={negativePrompt} onChange={e=>onChangeNegativePrompt(e.target.value)} placeholder="Elements to avoid..." rows={2} className="mt-1.5 w-full p-2.5 bg-zinc-50 border border-zinc-200 rounded-xl text-xs focus:outline-none resize-none"/>}</div>}</div>;
};
