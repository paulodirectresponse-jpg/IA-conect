import { apiRequest } from './apiClient.js';
import { Generation, GenerationRequestDraft } from '../types/index.js';
function normalizeReferences(draft:GenerationRequestDraft){const refs=draft.references||[],hasExplicitRoles=refs.some(r=>Boolean(r.role));return refs.map((r,index)=>{const role=String(r.role||'').toUpperCase();let slot_type:'INITIAL'|'END'|'GENERAL'='GENERAL';if(['START_FRAME','INITIAL_FRAME','INITIAL'].includes(role))slot_type='INITIAL';else if(['END_FRAME','END'].includes(role))slot_type='END';else if(!hasExplicitRoles&&draft.mode==='IMAGE_TO_VIDEO')slot_type=index===0?'INITIAL':index===1?'END':'GENERAL';return{asset_id:r.asset_id,slot_type,alias:r.alias_snapshot};});}
export const generationClient={
 async create(draft:GenerationRequestDraft):Promise<Generation>{const d:any=draft,s:any=draft.settings||{};return apiRequest<Generation>('/api/generations',{method:'POST',body:JSON.stringify({model_id:draft.model_id,mode:draft.mode,prompt:draft.prompt,negative_prompt:d.negative_prompt,duration_seconds:s.duration_seconds||1,resolution:s.resolution,aspect_ratio:s.aspect_ratio,number_of_outputs:s.number_of_outputs,seed:s.seed,motion_strength:s.motion_strength,audio_enabled:Boolean(s.audio_enabled),model_variant:s.model_variant,pricing_options:s.pricing_options,references:normalizeReferences(draft),client_request_id:draft.request_id,requested_provider_id:d.provider_id,authorized_credit_price:d.authorized_credit_price??d.retail_credit_price??draft.estimated_cost_cents,retail_pricing_id:d.retail_pricing_id,pricing_signature_hash:d.pricing_signature_hash})});},
 async get(id:string){return apiRequest<Generation>(`/api/generations/${id}`);},async list(max=50){return apiRequest<Generation[]>(`/api/generations?limit=${Math.min(100,max)}`);},async cancel(id:string){return apiRequest<Generation>(`/api/generations/${id}/cancel`,{method:'POST'});}
};
