export interface CreditAccount{user_id:string;available_credits:number;reserved_credits:number;total_credits:number;total_purchased_credits:number;total_promotional_credits:number;total_used_credits:number;total_expired_credits:number;total_revoked_credits:number;updated_at:string;available_balance_cents:number;reserved_balance_cents:number;total_balance_cents:number;}
export interface CreditLotAllocation{credit_lot_id:string;credits:number;net_cash_value_per_credit_micros:number;source:string;expires_at:string|null;}
export interface CreditTransaction{transaction_id:string;user_id:string;type:string;amount_credits:number;status:string;reference_type:string;reference_id:string;idempotency_key:string;lot_allocations:CreditLotAllocation[];balance_after_credits:number;created_at:string;metadata?:Record<string,any>;}
export interface PackVersion{pack_id:string;version:number;price_brl_cents:number;base_credits:number;bonus_credits:number;total_credits:number;active:boolean;effective_from:string;label:string;badge?:string;}
export interface RetailPricingVersion{retail_pricing_id:string;pricing_signature_hash:string;retail_credit_price:number;target_margin_percent:number;normal_floor_margin_percent:number;emergency_floor_margin_percent:number;version:number;active:boolean;}
